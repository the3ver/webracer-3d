{
  "name": "racer",
  "version": "0.1.0",
  "description": "3D Synthwave Arcade Combat Racer with Three.js",
  "type": "module",
  "scripts": {
    "dev": "vite src --port 3000",
    "build": "vite build src --outDir ../dist",
    "preview": "vite preview src --port 3000",
    "test": "playwright test"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.0.0"
  }
}